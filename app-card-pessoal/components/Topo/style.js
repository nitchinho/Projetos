import { StyleSheet } from "react-native";

const styleTopo = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
    },
    cardtopo: {
        flex: 1,
    }
});

export default styleTopo;